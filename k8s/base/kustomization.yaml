apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ingress.yaml

commonLabels:
  app: homecare-app
  version: v1.0.0

images:
- name: homecare-app
  newTag: latest
