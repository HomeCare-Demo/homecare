apiVersion: apps/v1
kind: Deployment
metadata:
  name: homecare-app
  labels:
    app: homecare-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homecare-app
  template:
    metadata:
      labels:
        app: homecare-app
    spec:
      containers:
      - name: homecare-app
        image: homecare-app:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
